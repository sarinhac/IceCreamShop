@model IEnumerable<IceCreamSystem.Models.CreditCard>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Company.NameCompany)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NameCreditCard)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DescriptionCreditCard)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.RateCreditCard)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Created)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Company.NameCompany)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NameCreditCard)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DescriptionCreditCard)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RateCreditCard)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Status)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Created)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.IdCreditCard }) |
                @Html.ActionLink("Details", "Details", new { id=item.IdCreditCard }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.IdCreditCard })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
